import { EgynationaIDPipe } from './egynationa-id.pipe';

describe('EgynationaIDPipe', () => {
  it('create an instance', () => {
    const pipe = new EgynationaIDPipe();
    expect(pipe).toBeTruthy();
  });
});
